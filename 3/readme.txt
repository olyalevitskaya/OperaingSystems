для того, чтобы скомпилировать программу, надо положить файл memory.c в папку apps, а затем приписать его в строку TARGETS в Makefile (который уже лежит в папке apps). Запускать make (он на уровень выше).  